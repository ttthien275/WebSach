@model IEnumerable<WebBanSach.Models.NHAXUATBAN>

@{
    ViewBag.Title = "Quản lý nhà cung cấp";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

@*<h2>Quản lý nhà cung cấp</h2>

<p>
    @Html.ActionLink("Thêm mới", "Create")
</p>
<table class="table">
    <tr>
        
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
           
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.MaNXB }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.MaNXB })
            </td>
        </tr>
    }

</table>*@

<div class="card">
    <div class="card-header">
        <div class="row">
            <div class="col-md-6">
                <strong class="text-danger text-uppercase">TẤT CẢ NHÀ CUNG CẤP</strong>
            </div>
            <div class="col-md-6 text-right">
                <a href="@Url.Action("Create","QLNhaCC")" class="btn btn-success btn-sm"><i class="fas fa-plus"></i>Thêm</a>
                <a href="Detrash" class="btn btn-danger btn-sm"><i class="fas fa-trash"></i>Thùng rác</a>
            </div>
        </div>
    </div>
    <div class="card-body">
        <table id="myTable" class="table table-bordered table-hover table-striped">
            <thead>
                <tr>
                    <th style="width:20px;" class="text-center">#</th>
                    <th>
                        Thương hiệu
                    </th>
                    <th>
                        Địa chỉ
                    </th>
                    <th>
                        Điện thoại
                    </th>
                    <th style="width:200px;" class="text-center">Chức năng</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>
                        <input name="STT" type="checkbox" class="text-center" />
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.TenNXB)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Diachi)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DienThoai)
                    </td>
                    <td class="text-center">
                        @* buttun trang thai(de day t dung copy neu can)
                @if (item.status == 1)
            {
                <a class="btn btn-success btn-sm" href="@Url.Action("Status","User_NV",new { id = item.id_NV })"><i class="fas fa-toggle-on"></i></a>
            }
            else
            {
                <a class="btn btn-danger btn-sm" href="@Url.Action("Status","User_NV",new { id = item.id_NV })"><i class="fas fa-toggle-off"></i></a>
            }

            public ActionResult Status(int? id)
            {
            if (id == null)
            {
            return new HttpStatusCodeResult(HttpStatusCode.BadRequest);
            }
            Product product=db.Product.Find(id)
            product.Status=(product.Status==1)?2:1;
            if(product.Status==1)
            db.Entry(product).State = EntityState.Modified;
            db.SaveChanges();
            return RedirectToAction("Index");
            }*@
                        <a class="btn btn-info btn-sm" href="@Url.Action("Edit","QlNhaCC",new { id = item.MaNXB })"><i class="fas fa-edit"></i></a>
                        @*<a class="btn btn-warning btn-sm" href="@Url.Action("Details","",new { id = item.MaNXB })"><i class="fas fa-eye"></i></a>*@
                        <a class="btn btn-danger btn-sm" href="@Url.Action("Delete","QlNhaCC",new { id = item.MaNXB })"><i class="fas fa-trash"></i></a>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </div>
</div>
@section header{

    <link href="~/Content/jquery.dataTables.min.css" rel="stylesheet" />
}
@section footer{
    <script src="~/Scripts/jquery.dataTables.min.js"></script>
    <script>
        jQuery(document).ready(function () {
            jQuery('#myTable').DataTable();
        });
    </script>
}