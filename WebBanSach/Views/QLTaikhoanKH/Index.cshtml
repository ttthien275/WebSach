@model IEnumerable<WebBanSach.Models.TAIKHOAN>

@{
    ViewBag.Title = "Quản lý tài khoản khách hàng";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="card">
    <div class="card-header">
        <div class="row">
            <div class="col-md-6">
                <strong class="text-danger text-uppercase">TẤT CẢ TÀI KHOẢN KHÁCH HÀNG</strong>
            </div>
            <div class="col-md-6 text-right">
                <a href="@Url.Action("Create","QLTaikhoanKH")" class="btn btn-success btn-sm"><i class="fas fa-plus"></i>Thêm</a>
                <a href="Detrash" class="btn btn-danger btn-sm"><i class="fas fa-trash"></i>Thùng rác</a>
            </div>
            <div><br /></div>
        </div>
    </div>
    <div class="card-body">
        <table id="myTable" class="table table-bordered table-hover table-striped">
            <thead>
                <tr>
                    <th style="width:20px;" class="text-center">#</th>
                    <th>
                        @Html.DisplayNameFor(model => model.ID_LOAITK)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.HOTEN)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.USERNAME)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.MATKHAU)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.EMAIL_TK)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.DIACHI_TK)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.SDT)
                    </th>
                    <th>
                        @Html.DisplayNameFor(model => model.STATUS)
                    </th>
                    <th style="width:200px;" class="text-center">Chức năng</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var item in Model)
                {
                <tr>
                    <td>
                        <input name="STT" type="checkbox" class="text-center" />
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.ID_LOAITK)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.HOTEN)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.USERNAME)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.MATKHAU)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.EMAIL_TK)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.DIACHI_TK)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.SDT)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.STATUS)
                    </td>
                    <td class="text-center">
                        @* buttun trang thai(de day t dung copy neu can)
                @if (item.status == 1)
            {
                <a class="btn btn-success btn-sm" href="@Url.Action("Status","User_NV",new { id = item.id_NV })"><i class="fas fa-toggle-on"></i></a>
            }
            else
            {
                <a class="btn btn-danger btn-sm" href="@Url.Action("Status","User_NV",new { id = item.id_NV })"><i class="fas fa-toggle-off"></i></a>
            }

            public ActionResult Status(int? id)
            {
            if (id == null)
            {
            return new HttpStatusCodeResult(HttpStatusCode.BadRequest);
            }
            Product product=db.Product.Find(id)
            product.Status=(product.Status==1)?2:1;
            if(product.Status==1)
            db.Entry(product).State = EntityState.Modified;
            db.SaveChanges();
            return RedirectToAction("Index");
            }*@
                        <a class="btn btn-info btn-sm" href="@Url.Action("Edit","QLTaikhoanKH",new { id =item.ID  })"><i class="fas fa-edit"></i></a>
                        <a class="btn btn-warning btn-sm" href="@Url.Action("Details","QLTaikhoanKH",new { id = item.ID })"><i class="fas fa-eye"></i></a>
                        <a class="btn btn-danger btn-sm" href="@Url.Action("Delete","QLTaikhoanKH",new { id = item.ID })"><i class="fas fa-trash"></i></a>
                    </td>
                </tr>
                }
            </tbody>
        </table>
    </div>
</div>
